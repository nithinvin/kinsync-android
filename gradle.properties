org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Public backend base URL for Phase-1 health check (see docs/design.md).
# Override locally by adding KINSYNC_BASE_URL=... to local.properties if needed.
KINSYNC_BASE_URL=https://kinsync.ddns.net
